import kotlin.Int;

CREATE TABLE Countries (
    id TEXT NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    population INTEGER AS Int NOT NULL
);

upsertCountry:
INSERT OR REPLACE INTO Countries(id, name, population)
VALUES(:id, :name, :population);

getCountriesList:
SELECT * FROM Countries;

getCountryById:
SELECT * FROM Countries WHERE id = ?;
